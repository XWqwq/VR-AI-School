# Dify 校园知识库构建状态

更新时间：2026-07-18

## 已完成

知识库源文件为 `打包/知识库.zip`，已在本机 Dify 工作区拆分为三个知识库：

| Dify 知识库 | UUID | 已完成索引 |
|---|---|---:|
| 虚拟校园-教学学业 | `df3b2e86-e54e-47c9-bc84-bcbac12c5010` | 73 |
| 虚拟校园-校园办事 | `884e8b80-b969-4b12-8dfa-a841f7b359ba` | 22 |
| 虚拟校园-规章制度 | `2d391d55-abb2-44bc-98aa-204cdb89ea9a` | 12 |

共 107 份 DOCX/PDF 文档完成索引，索引错误为 0。工作流 YAML 的知识检索节点已更新为以上三个 UUID。

当前使用 Dify `economy` 索引方式，不依赖云端 Embedding 或 Rerank 模型。后续配置本地 Embedding 后，可以升级为高质量向量索引。

## 未导入资料

Dify 1.14.2 的文件上传接口拒绝以下 6 个旧版 `.doc` 文件：

- 《半导体器件物理》.doc
- 《半导体物理及固体物理基础》.doc
- 《数字集成电路设计》.doc
- 《电磁场与电磁波》.doc
- 《电路分析》.doc
- 2024年苏州大学学生意外保险简介.doc

将这些文件另存为 DOCX 后，可使用增量导入脚本补传。压缩包中的嵌套 RAR 不属于 Dify 可直接解析的知识文档，未导入。

## 增量导入

导入工具：`打包/tools/import_dify_knowledge.py`

工具会复用同名知识库、跳过同名文档、上传新增资料、更新工作流 YAML，并生成非敏感导入清单。它要求通过环境变量临时提供 `DIFY_DATASET_KEY`，不会把密钥写入项目。

## Dify 与 Unity 接入状态

- 已安装 Dify 官方 Ollama `1.0.0` 插件。
- 已添加并验证本机 LLM `qwen2.5:7b`；校园/闲聊由零模型开销的关键词路由判断，闲聊和校园知识总结统一使用 7B，避免模型切换并提高回答质量。
- Ollama 地址为 `http://host.docker.internal:11434`。
- 已导入并发布 `advanced-chat` 应用，App ID 为 `370c497b-30be-4a27-92b8-b55a5bd92072`。
- 已生成独立的应用 API Key，并写入 Unity 的 `Application.persistentDataPath/dify_config.json`。
- Unity Assets、工作流 YAML、项目文档和导入清单均未保存 API Key。
- 已完成 Dify API 端到端问答测试，知识检索和本地模型回答正常。
- Ollama `think` 已关闭，避免在 VR 通知书中显示思考过程。
- 当前单模型为 `qwen2.5:7b`，上下文限制为 8192、最大回答 256 Token；Ollama 保活 10 分钟，启动测试会预热模型。
- 实测预热后的校园知识回答约 0.9～1.2 秒，模型驻留约 2.4 GB；首次冷加载仍可能需要数秒。
- 通知书回答要求 120 个汉字以内，Unity 端每页最多约 110 字，超出时使用“上一页 / 下一页”阅读，不再直接截断。
- 对话流程已调整为“问题分类 → 闲聊直接回答 / 校园问题检索知识库 → 校园知识回答”，闲聊不会再触发知识检索。
- 校园知识检索使用本地 economy 关键词索引，Top K 为 5；回答节点会忽略低相关片段，并优先采用正式规章、办事指南和更新资料。
- 可从 Dify 工作流节点日志核对知识检索实际命中的文档。

运行顺序：启动 Ollama，启动 Docker Desktop 和 Dify，然后进入 Unity Play/PDC。Unity 会优先使用已配置的 Dify；Dify 不可用时再回退本机 Ollama，最后回退 Mock 服务。
# 2026-07-19 增量补充

- 用户提供的 `知识库.zip` 已复核：107 份 PDF/DOCX 与现有 Dify 数据集一致，脚本全部判定为已存在，未重复上传。
- 新增 `虚拟校园体验地点与操作说明.md`，已上传至“虚拟校园-校园办事”数据集。
- Dify 索引状态已验证为 `completed`，文档统计 776 个词元；检索测试能够命中该文档。
- 导入工具现在支持 `.md` 与 `.txt`，方便后续直接维护轻量知识条目。
- 原压缩包仍含 6 份旧版二进制 `.doc`。其中《电路分析》已有同名 DOCX 版本；其余文件需要先在 Word 中另存为 DOCX，再增量导入。自动转换时 Word 兼容层卡住，因此没有上传损坏或未完成文件。
